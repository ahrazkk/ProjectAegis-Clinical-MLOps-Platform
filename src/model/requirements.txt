# DDI Model Dependencies
# Based on MCR III Model Specification
# Dependencies pinned to specific versions to reduce supply-chain security risks
# Update these versions only after explicit review and testing

# Core ML Framework
# Security: Using torch>=2.6.0 to address CVEs:
# - CVE-2024-xxxxx: Heap buffer overflow (fixed in 2.2.0)
# - CVE-2024-xxxxx: Use-after-free (fixed in 2.2.0)  
# - CVE-2024-xxxxx: torch.load RCE vulnerability (fixed in 2.6.0)
torch==2.6.0

# Security: Using transformers>=4.48.0 to address multiple deserialization CVEs
# - CVE-2024-xxxxx: Deserialization of untrusted data (fixed in 4.36.0, 4.48.0)
transformers==4.48.0

# Hugging Face PubMedBERT specific
sentencepiece==0.2.0

# Scientific Computing
numpy==1.26.4
scikit-learn==1.5.2
pandas==2.2.3

# Training Utilities
tqdm==4.67.1

# Logging and Configuration
pyyaml==6.0.2

# For Vertex AI Vizier integration (optional)
# google-cloud-aiplatform>=1.25.0

# Development/Testing
pytest==8.3.4
pytest-cov==6.0.0
